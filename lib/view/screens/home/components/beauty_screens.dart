import 'package:flutter/material.dart';

class BeautyScreens extends StatelessWidget {
  const BeautyScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
